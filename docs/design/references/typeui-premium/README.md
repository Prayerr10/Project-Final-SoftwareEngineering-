# TypeUI Premium Design Reference

This folder stores the external TypeUI Premium design reference used as guidance for the Premium UI redesign.

It is not one of the 15 required academic AI skills in `skills/`.

This reference is not used by application runtime, database schema, API logic, tests, CI, or Cloudflare deployment.

It does not contain a secret, credential, token, or environment value.
